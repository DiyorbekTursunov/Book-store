interface bookData {
    name: string,
    categoryName: string,
    description: string,
    imageUrl: string,
    price: string,
    categoryId: string
}

interface categoryData {
    title: string
}